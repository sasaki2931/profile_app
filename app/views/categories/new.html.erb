<h1>カテゴリー登録</h1>

<% if @category.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@category.errors.count, "error") %> prohibited this <%= @category.name %> from being saved:</h2>
    <ul>
    <% @category.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_with(model: @category, local: true) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit "カテゴリー追加" %>
<% end %>